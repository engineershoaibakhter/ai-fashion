/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s3-us-west-2.amazonaws.com'], // Add the hostname(s) of your image sources
      },
};

export default nextConfig;



